/* tslint:disable:no-unused-variable */

import { PonyImagePipe } from './pony-image.pipe';

describe('Pipe: PonyImagee', () => {
  it('create an instance', () => {
    let pipe = new PonyImagePipe();
    expect(pipe).toBeTruthy();
  });
});
